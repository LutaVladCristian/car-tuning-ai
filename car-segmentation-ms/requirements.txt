# PyTorch — install the appropriate variant BEFORE this file (see README).
# GPU (CUDA 11.8): pip install torch==2.2.0 torchvision==0.17.0 torchaudio==2.2.0 --index-url https://download.pytorch.org/whl/cu118
# CPU only:       pip install torch==2.2.0 torchvision==0.17.0 torchaudio==2.2.0 --index-url https://download.pytorch.org/whl/cpu
torch==2.2.0
torchvision==0.17.0
torchaudio==2.2.0

# ML / vision
numpy<2
opencv-python-headless
pillow
ultralytics
git+https://github.com/facebookresearch/segment-anything.git

# API / server
fastapi
uvicorn[standard]
python-multipart
python-dotenv
openai
requests
charset-normalizer

# GCS — model weight download at startup
google-cloud-storage>=2.16
