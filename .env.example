# =============================================================================
# car-backend-ms  (FastAPI auth/proxy - port 8001)
# =============================================================================

# PostgreSQL connection string. For local dev SQLite is fine: sqlite:///./car_backend.db
DATABASE_URL=sqlite:///./car_backend.db

# URL of the segmentation microservice (used by the backend proxy)
SEGMENTATION_MS_URL=http://localhost:8000

# Production hardening: when true, backend fails closed if it cannot fetch
# a Cloud Run IAM identity token for the segmentation service.
REQUIRE_SEGMENTATION_IAM=false

# Per-backend-instance safety valve for expensive GPU/OpenAI edit calls.
EDIT_PHOTO_RATE_LIMIT_PER_HOUR=20

# Firebase Storage bucket - stores user photo artifacts.
# Local dev: start `firebase emulators:start --only storage` and set FIREBASE_STORAGE_EMULATOR_HOST=127.0.0.1:9199
FIREBASE_STORAGE_BUCKET=your-project-id.firebasestorage.app
FIREBASE_STORAGE_EMULATOR_HOST=

# Firebase / Google Identity Platform project used to verify user ID tokens
FIREBASE_PROJECT_ID=your-gcp-project-id

# =============================================================================
# car-segmentation-ms  (FastAPI ML service - port 8000)
# =============================================================================

# OpenAI API key - required for the /generate-photo endpoint
OPENAI_API_KEY=sk-...

<<<<<<< HEAD
# Optional locally. Required in Cloud Run to download missing model weights.
MODEL_BUCKET=car-tuning-ai-vision-models

=======
>>>>>>> 81e17ab068b83971db188396d0af1845e2a8bef3
# =============================================================================
# car-frontend  (Vite + React SPA - dev port 5173, prod port 80)
# =============================================================================

# Backend base URL consumed by Vite at BUILD TIME.
VITE_API_BASE_URL=http://localhost:8001

# Firebase web app config consumed by Vite at BUILD TIME.
VITE_FIREBASE_API_KEY=your-web-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-gcp-project-id
VITE_FIREBASE_APP_ID=your-firebase-app-id
